#!/bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
NODE_OPTIONS=--max_old_space_size=8192

$DIR/../node_modules/.bin/ts-node --project="$DIR/../tsconfig.json" $DIR/../index.ts "$@"
